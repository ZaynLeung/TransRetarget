# config
variables_define.py 存放全局变量 更换数据集或者机器手模型时候需要修改
# dataset
## data
存放了数据集和机器手模型文件 
## data_collect
存放数据集获取和处理代码
## dataset.generators 
数据加载器
# LinkerHand
是灵心巧手的驱动库，在实机测试时候使用
# model
模型文件夹，定义了网络结构

# scripts
- **main_train.py**: 是训练代码
- **main_test.py** 是测试代码
- **main_h5_simulate.py**: 是h5数据集仿真测试代码
- **main_h5_realtlime.py**: 是h5数据集实机测试代码
- **main_visionpro_realtime.py**: 是头显实机测试代码
- **main_visionpro_multi_hand.py**: 是多手头显实机测试代码
- **check_gym.py**: 测试仿真环境。注意：如遇 urdf 路径报错，需检查 urdf 文件中 mesh 路径的斜杠方向（与操作系统相关），并核对 `yumi_gym/yumi_env.py` 中的 urdf 文件路径是否与实际路径一致。
- **check_hand_connect.py**: 测试linker手部连接。注意：需确认连接的手部类型，在 `_init_hands` 方法中修改 `hand_type` 为 "left" 或 "right"。
- **check_model.py**: 测试模型参数是否正常。
- **check_visionpro_connect.py**: 测试头显连接是否正常。注意：需修改脚本中的 IP 地址以确保连接正确。
